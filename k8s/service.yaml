apiVersion: v1
kind: Service

metadata:
  name: college-event-service

spec:
  type: NodePort
  selector:
    app: college-event

  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
